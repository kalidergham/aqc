# Sound effects (optional)

Place optional sound files here (e.g. `move.wav`, `capture.wav`,
`brilliant.wav`). Sound playback is an optional enhancement; the application
runs fully without any audio assets.
